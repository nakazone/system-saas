---
description: UTF-8 encoding — never introduce corrupted characters (U+FFFD)
alwaysApply: true
---

# UTF-8 encoding (mandatory)

All source files in this repo are **UTF-8** (no BOM).

## Never write corrupted characters

- Do **not** use U+FFFD or mojibake like `Ã§`, `Ã£`, `nÃ£o`, `VisÃ£o`.
- Use proper Unicode:
  - Em dash: `—` (titles, separators)
  - En dash: `–` (ranges: `500 – 999`)
  - Middle dot: `·` (inline separators)
  - Ellipsis: `…` (loading states: `A carregar…`)
  - Portuguese accents: `ã`, `ç`, `á`, `é`, `í`, `ó`, `ú`, `õ`, `â`, `ê`, `ô`, `à`, `Á`, `Ú`, etc.

## HTML

Every HTML file must include in `<head>`:

```html
<meta charset="UTF-8" />
```

## Before finishing edits

Ensure all text files decode as UTF-8 (Nixpacks/Railway fail the build otherwise).

## Saving files

- Line endings: LF (`\n`)
- Encoding: UTF-8
